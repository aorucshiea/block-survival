# 方块生存 v0.1 · 试玩诊断报告

> 诊断方式：3723 行全量代码通读 + 两轮 Playwright 自动化实机试玩（1366×768，含截图与 DEBUG 钩子数值采集）。
> 测试种子 102557768；加载 1.4s；场景 46 draw calls / 26.6 万三角面（性能无压力）。

## 结论一句话

核心循环「挖（10cm 雕刻）→ 攒料（质量守恒）→ 造（合成/放置）」成立且手感系统完成度远超 v0.1 的自我定位，但存在 1 个必现 P0 崩溃 bug、1 个致命产品缺口（无存档）和一条断掉的经济链（煤/铁无去处、肌肉系统不兑现）。

---

## 🔴 P0：必现 bug（可直接复现）

### 1. 雨天每帧抛 ReferenceError，天气系统整体断电
- **位置**：`updateWeather()` 雨滴落地区块（约 3641 行）。`surf` 变量只在 `if (w.snow)` 分支内用 `const` 声明，紧跟的 `else if` 雨分支直接引用 → `ReferenceError: surf is not defined`。
- **后果链**：异常从粒子循环中段抛出 → 后续雨滴不更新、`a.needsUpdate=true` 永远不执行 → **雨点视觉冻结/不显示**；湿痕、水花特效全无；函数末尾的天气文字更新跳过 → **右上角天气栏在雨天冻结在旧天气**。实测一次雷雨采集到 130+ 条报错。
- **修法**：把 `const gx/gz/gy/surf` 的计算提到 if/else 之前（两种天气共用），一行结构修复。
- **复现**：玩到下雨（天气 60~180s 自动轮换），F12 控制台看报错刷屏。

### 2. 放方块大量静默失败
- **位置**：`place()` 的失败分支：目标格被占、放置格与玩家身体相交、越界等 → 直接 `return`，无 toast、无音效、无抖动。
- **复现**：站着低头瞄准脚下方块顶面按中键 → 什么都没发生，也没有任何解释。自动化实测两次命中（工具箱放置失败，手中物品未消耗，零反馈）。
- **修法**：至少给 aimTip/toast 反馈「这里放不下 / 会卡住自己」。

### 3. Esc 暂停依赖指针锁（边缘场景）
- Esc 无 keydown 处理器，只靠「Esc 退出指针锁 → pointerlockchange → 暂停」。浏览器拦截重锁时（连续 Esc、alt-tab 后），Esc 无效，玩家卡在无锁定的游戏状态。真实浏览器大多正常，P2。

---

## 🟠 结构缺口（设计层，按优先级）

| # | 问题 | 依据 | 建议 |
|---|------|------|------|
| 1 | **没有存档**。刷新=世界重随机+全部进度清零；连音量都存了 localStorage，世界却不存 | 代码：SEED 每次加载随机，无任何序列化 | 自动存档（种子+修改差异+玩家状态）进 localStorage，这是「玩具→游戏」的分界线 |
| 2 | **零威胁、无昼夜**。生存张力全靠自伤（摔/绊/饿/冻）；没有夜晚，建家没有防御理由——而粘合剂建筑物理是全游戏最好的钩子，现在没人需要它 | 代码无 mob/时间循环 | 先加昼夜+夜间气温下降（复用体温系统，零新系统），再谈怪物 |
| 3 | **二级材料经济断链**。煤料零用途；铁料唯一去处是铁镐，且效力仅比石镐高 15%（1.69→1.94）。目标 11「打铁镐」完成后铁=死料 | RECIPES 全表核对 | 给煤/铁加去处（火把/熔炉→熟食/铁锤），或提高铁的工具效力差距 |
| 4 | **肌肉/体脂系统悬空**。目标 13 让玩家「练肌肉」，实际肌肉只影响产热 ±6%/10kg 和一行展示文本；挖掘速度只看等级 | `survivalTick` + `strengthMult` 核对 | 把肌肉接进负重/工具耗力/挖掘，或从目标文案删掉承诺 |
| 5 | **目标链不教生存**。13 个目标全是合成线，吃喝/体温零引导；玩家可能 40 分钟不碰水然后突然渴死 | GOALS 表 | 插入 2 个生存目标（「喝一次水」「吃一口东西」） |
| 6 | **室内不保温**。体温只看全局气温，房子对寒冷/中暑零收益，唯一价值是不淋雨（纯视觉） | `survivalTick` 的 curTemp 为全局值 | 按头顶遮挡判定「室内」，寒冷积累打折——盖房子才有生存理由 |

---

## 🟡 手感 / UI（实测确认）

1. **合成界面标题错位**：「材料袋」标题下紧跟「打工具」标题，然后是工具卡，材料袋格子垫底——标题与内容交错，阅读断层（截图确认）。
2. **左上角提示 130 字长跑马灯**：新手过载；建议按目标进度逐步解锁（做工具前只显示移动+挖掘）。
3. **质量格式混排**：「木料 0.0g/2.8 kg」，g 与 kg 混用不一致。
4. **HUD 无材料常显**：石料存量要开 E 才知道；建议力量面板加一行 top-3 材料。
5. **雷雨没有雷**：只有调暗+雨（雨天还因 bug 不显示），无闪电/雷声/雨声——天气是哑剧。
6. **无罗盘/地图**：4 个村庄、井全靠迷路；`villageSpots` 数据现成，加一个「最近村庄方向」HUD 箭头成本极低。
7. 第一人称手臂是素色盒子，与体素世界质感脱节（低优先）。

---

## ✅ 做对了的（保持，别动）

- **10cm 子格雕刻 + 质量守恒经济**：挖=按密度得 kg 材料，放=体积×密度扣料，小方块 2.5g 也能精确结算——差异化核心，数学自洽。
- **粘稠度塌方 + 未粘合放置物被撞飞**：建筑有了物理重量，市场上罕见。
- **反馈通道**：咬口高亮框随工具变大、每口音效、力竭红条、挖土要蹲的 aimTip 场景化提示，完成度高。
- **性能**：256 区块 46 draw calls / 27 万面，软渲染都能跑，真 GPU 无压力；「撞墙反弹按材质弹性」这类细节手感出色。

---

## 你来人工验证的清单（每条 ≤1 分钟）

1. 玩到下雨，F12 看控制台：是否刷 `surf is not defined`；右上角天气文字是否卡住不动。（预期：是）
2. 站着低头瞄准脚下顶面按中键 → 是否毫无反应；再对远处侧面放 → 正常放置。（预期：前者静默失败）
3. Shift+W 全速跑 1 秒以上，突然按 A 或 D → 是否绊倒扣血。（自动化未触发，待人工确认）
4. 挖到深层打铁镐，与石镐对比挖石头的手感差距是否值回挖铁成本。（预期：差距不明显）
5. 雨雪天站进盖好的房子 → 寒冷条是否照常积攒。（预期：照常，即房子不保温）
6. 吃蘑菇+持续挖矿 10 分钟 → 肌肉数字是否变化、变化后有无任何手感差异。（预期：数字会动，手感无差异）

## 建议修复顺序

`surf` 一行修复 → 放置失败反馈 → 合成界面标题归位 → localStorage 存档 → 目标链插入生存教学 → 昼夜+夜间降温 → 煤/铁的 sink。
