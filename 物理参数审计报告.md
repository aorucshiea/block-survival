# 方块生存 v1.0-wip · 物理参数审计（TRAE 改造后）

> 审计对象：E:\zcode\方块生存.html（4573 行，TRAE 增改 929 行后的快照，12:47 版本，仍在修改中）
> 方法：1446 行 diff 全读 + 派生数值手工验算 + 无头实机跑 `DEBUG.selfTest()`

## 实测状态

- 启动干净：0 报错，加载 2.4s
- 自带自测：**16 通过 / 0 失败**
- 性能：**110 draw calls（预算<120）、73.7 万三角面（预算<80万）——达标但余量只剩 8%/8%**。主因是蘑菇合并网格放弃了视锥剔除（全图蘑菇常驻显存绘制）。GPU 上没问题，核显老机可能掉帧，留个观察点

## ✅ 算下来合理的（保持别动）

| 参数 | 验算 | 结论 |
|------|------|------|
| 跳跃 5.1²/48 | =0.542m ≈ 注释"约50cm" | ✓ |
| 昼夜 840s | 白462/昏63/夜273/黎42，首夜开局4.6分钟到达，两小时≈8.5天 | ✓ 教学窗口成立 |
| 火把寿命 240s < 夜273s | "一支撑不完一夜，要备两支" | ✓ 有意的资源摩擦 |
| 煤经济 | 1 煤矿 2.6kg = 32 支火把 = 128 火把分钟 ≈ 8 个夜的量 | ✓（甚至太富裕，见问题2） |
| 雪夜户外 | -18°C 冻满 100 需 5.2min > 夜长 4.5min——刚好冻不死，白天 3.3min 缓回 | ✓ 硬核但不背锅；营火 3.5/s 回温能救命 |
| 室内判定 ×0.45 + 夜温减半 | 雨夜室内积累 7.7/夜 vs 户外 34/夜 | ✓ 盖房子收益真实 |
| 营养闭环 | 每天耗约 2230kcal ≈ 5.3 块烤肉；第5夜 7 怪 ≈ 10 生肉 ≈ 自给 | ✓ 打怪=食物链成立 |
| 肌肉 effortMult 42/(10+m) | 32kg=1.00 / 42kg=0.81 / 60kg=0.60 | ✓ 公式对，注释错（见下） |
| 玩家攻击距离 3.4m > 怪 1.25m | 你打得到它、它够不着你（合理的战斗规则） | ✓ |
| 存档三断面 | 死亡/刷新(beforeunload+visibilitychange)/换世界显式删档，版本校验宁丢不脏 | ✓ 架构正确 |

## 🔴 问题 1（P0·系统打架）：啃一口就能穿过任何墙——"铁=防御材料"不成立

- 机制冲突：`doBite` 咬 1 个子格后 `carved` 有记录 → `blocksPlayer()` 对整个格子放行（原设计给玩家用的，玩家有 10cm 子格精细碰撞兜底）。**但怪没有子格碰撞**，`mobBlocked` 只调 `blocksPlayer` → **一口咬掉 1cm³，整面墙就变幽灵可穿**
- 实际后果：木板墙 0.72s 穿透、石墙 1.2s、"铁墙"（铁矿石块 4.5 硬度）也只要 1.8s。注释里写的"铁=防御材料"完全不成立；**粘合过的墙同样被啃**（啃墙分支没有排除 glued），但黄昏提示却说"没粘合的墙挡不住它们"——暗示粘合墙挡得住，误导
- 修法（三选一）：① `mobBlocked` 对 `carved` 且 `n < 500`（咬掉不到半）的格子仍视为实心；② 怪对啃过的格子做子格碰撞；③ 怪啃墙每次 `doBite` n=3 且要求同一格累计 ≥5 口才放行

## 🔴 问题 2（P1·平衡）：火把 = 无敌结界，夜晚威胁归零

- 几何验算：怪在距火 0.85×4.5=3.8m 处掉头逃跑，攻击距离 1.25m。玩家只要站在火把 2.6m 内，怪从任何方向进入攻击距离前必先进恐惧圈——**永远摸不到你**
- 而火把便宜到离谱：1 块煤 = 32 支 = 8 个夜晚的绝对安全。夜行者数值再怎么爬（cap 8 只）都无意义
- 修法建议：① 恐惧只在 70% 生效（怪有 30% 概率"饿红了眼"硬冲 2 秒）；② 或火把煤耗 ×3；③ 或恐惧半径缩到 3m 并允许怪以 1.35 倍速突进穿越——保留"火=安全区" fantasy，但不再绝对

## 🔴 问题 3（P1·bug）：怪的击退被自己的 AI 覆盖

- `tryAttack` 里 `best.vx += d.x * 4.5`，但 `updateMobs` 每帧 `m.vx = dirX * spd` 直接重写——击退只活 16ms（移动 7cm），视觉上完全没击退。玩家的击退（P.vel）有动量所以有效，怪的没有
- 修法：给怪加 `m.kvx/m.kvz` 击退速度通道，每帧叠加并 ×0.85 衰减

## 🟡 注释与数学不符（不炸，但会误导后来调参的人）

1. `atk 8=夜里挨三下会死` —— 错。100 血 ÷ 8 = **13 下**，攻击间隔 1.3s，需要连续贴身 17 秒。实际单怪威胁很低（多怪围殴才致命），建议注释改掉或 atk 提到 12~15
2. `铁大锤约 28` —— 实际 3+2.588×7+3.56 ≈ **24.7**
3. 肌肉"幅度克制（±25%）" —— 公式在极值 15kg 时是 **+68%**、60kg 时 -40%。实际游戏里肌肉很少到极值，但注释和公式对不上
4. "雪原夜 -18°C" —— 温度是全局的：**任何生物群系的雪夜都是 -18°C**，不是雪原专属。评论误导，行为本身可接受（雪夜约每 2.8 小时游戏时间来一次）

## 🟡 小漏洞两个

- 怪啃墙掉的材料进**玩家**的材料袋（`doBite` 统一记 frags）——怪帮你拆墙你还赚木料，建议怪啃墙不进账
- 夜怪不存档：刷新后夜清空、刷怪计时重置——玩家可用刷新白嫖平安夜。已知限制就注释写明，或者连怪一起存

## 结论

参数面的大盘是健康的：所有新数值有 rationale、会话弧线（14 分钟一天、首夜 4.6 分钟、两小时 8.5 天）成立、经济闭环（煤→火→肉→蛋白→肌肉→省体力）在数学上自洽。**把问题 1、2 修掉，这套生存循环就真的立住了**——问题 1 是 bug 级（墙=装饰），问题 2 是设计级（夜=无威胁）。
