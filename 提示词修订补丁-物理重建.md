# 《物理重建提示词》修订补丁（用前必读，直接粘贴到对应位置）

> 结论：该提示词骨架可用（阶段0审计先行 + 分阶段 + 回归清单 + 12 条验收场景，都是好结构）。
> 但有 4 处必须先改，否则 AI 会带着错误先验进场。以下按提示词原文顺序给补丁。

---

## 补丁 1：替换开头的【现状】段（去掉"疑似不可靠"的错误先验）

原文：
> 现状：UI 与表面反馈已基本正常，但内核物理系统存在重大问题（碰撞、子方块挖掘/放置、重力支撑、状态机、固定步长等疑似不可靠）。

替换为：
> 现状：物理内核已验证基本健康——10cm 子格雕刻、分轴碰撞（1/90s 子步）、粘稠度塌方、昼夜/火光/夜行者/存档均已实现且 DEBUG.selfTest() 46 项全过（版本 110，约 5100 行）。已知问题集中在：①夜行者三 bug（啃一口穿墙 P0、火把=无敌结界、击退被 AI 覆盖——见《物理参数审计报告.md》E:\zcode\物理参数审计报告.md，三个都还没修）；②体脂增速失控（满胃 0.64%/s）；③快捷栏把布尔值当数量渲染；④一批 UI 细节。你的任务是审计核实 + 按本规格修复补齐，不是推倒重建。

## 补丁 2：2.2/2.3 的"重建"降级为"验证 + 补累加器"

原文（2.2）：
> 固定 timestep（建议 1/60s）+ 累加器驱动所有物理……禁止用可变帧 dt 直接积分碰撞。

改为：
> 物理已有 1/90s 子步（physics() 的 steps = ceil(dt/(1/90))）。缺的是标准累加器语义：dt 超过 50ms 会被钳制导致低帧时间膨胀。任务：改为固定步长累加器（1/60s 或保留 1/90s）+ 上限防死亡螺旋，物理/渲染解耦。验收不变：30fps 与 60fps 同输入位移误差 <5%。**禁止以"重建"为名替换现有分轴碰撞与子步框架。**

原文（2.3）：
> AABB/胶囊碰撞体 + 扫掠（sweep/连续检测）+ 子步长，禁止高速穿模

改为：
> 保留现有 AABB 分轴碰撞 + 子步。仅在验收场景 1（疾跑撞墙 10 次）不过时才引入扫掠——先证明必要，再动手。

## 补丁 3：2.4 稳定值与现有塌方系统分工（二选一会打架）

原文：
> 支撑与粘合：未粘合墙有稳定值；夜行者撞击/啃咬扣稳定值；归零倒塌为掉落物；粘合剂提高上限……

改为：
> 重力支撑继续由现有粘稠度洪泛系统（supported/cohesionOf）负责，**不动**。"稳定值"仅作为未粘合/粘合方块对夜行者冲击与啃咬的**局部耐久条**：撞一下扣一格、啃按硬度扣，归零才走 knockBlock（未粘合）或变成可被啃穿的破口（粘合，2 倍咬数）。两系统分工写进注释：洪泛=重力真实感，稳定值=攻防数值。晃动/裂缝/声音反馈绑定稳定值，照原文执行。

## 补丁 4：P0 清单去伪存真 + 并入漏掉的三个真 P0

**划掉这三条（已实现，让 AI 验证即可，别重做）**：
- 第 9 条"死亡时立即存档"——已有：死亡→重生→saveGame()
- 第 7 条前半"F5 或可靠拦截"——已有 preventDefault
- 第 8 条"一天 14 分钟改为 12 分钟"——DAY_LEN=840 且已有黄昏 63s/黎明 42s 分段，文案与代码一致

**改预期（第 2 条）**：
- "To show your cursor, press Esc" 是浏览器指针锁的系统 UI，代码无法改语言；改为"减少指针锁意外丢失（进入 UI 时主动 exitPointerLock 而非丢焦点），触发频率为验收指标"

**并入三个审计已确认、最新版仍未修的 P0/P1（排在全部 P0 之前）**：
- P0-0：怪啃 1 子格后 blocksPlayer 整格放行 → 任何墙 0.72~1.8s 穿透，粘合墙同样。修法：mobBlocked 对 carved 且 n<阈值（建议 500）视为实心，或啃同一格 ≥5 口才放行；粘合墙 2 倍咬数
- P1-0：火把=无敌结界（恐惧圈 3.8m > 攻击可达 1.25m + 煤太便宜）。修法择一：恐惧 70% 生效+30% 饿红眼突进 2s / 煤耗×3 / 恐惧半径 3m 允许穿越。验收：夜 5 火边挂机 30s 必须可能被命中
- P1-1：怪击退被 AI 每帧重写（vx+=4.5 只活 16ms）。修法：m.kvx/kvz 击退通道 ×0.85/帧衰减
- P2：怪啃墙材料入玩家口袋（doBite 统计 frags）——加来源参数，怪啃不入账

## 补丁 5：阶段 3 合并既有的《TRAE物理系统优化-提示词.md》第三/四批

阶段 3 清单追加（内容见 E:\zcode\TRAE物理系统优化-提示词.md，这里只列条目）：
- 材料属性：可燃性 burn（蔓延挂 0.5s 节拍，燃烧=质量守恒唯一例外，注释留痕）+ 导热性 k（屋顶加权保温，木板屋 vs 铁皮屋验收差 ≥50%）
- MC 实践 4a：床（有怪不能睡）、村庄战利品箱（固定掉落）、树苗（木材可再生）
- MC 实践 4b：体素光照场（15 级 BFS 增量更新、光照<6 刷怪、烘顶点色；帧率回归 5% 即回退——唯一允许撤的条目）
- 明确不抄：红石、液体流动、村民交易、附魔、盔甲

## 补丁 6：交付要求追加两条

- `DEBUG.selfTest()` 以现有 46 项为基线，修复与新系统后扩到 ≥55 项，全绿才算交付。
- 每阶段结束 git commit（游戏目录已是 git 仓库，main 分支），commit message 中文简述。
