# 任务：优化《方块生存》世界物理系统（修 bug + 补材料属性）

## 你的上下文
- 游戏：`E:\zcode\方块生存.html`（单文件 + 本地 three.min.js，约 4600 行）。你上一轮加的昼夜/火光/夜行者/存档已通过 16 项自测。
- **先读 `E:\zcode\物理参数审计报告.md`**——里面的 3 个问题就是本任务的第一批工作，验算数据都在里面。
- 已验证、一根手指都不许动的核心数学：10cm 子格雕刻、质量守恒、粘稠度塌方洪泛、攀爬/绊倒/撞墙反弹。
- 性能现状：110 draw calls（预算 120）、73.7 万三角面（预算 80 万）——**余量只剩 8%，新逻辑不许加常驻网格/光源，蔓延类计算挂 0.5s 节拍（updateEnvTick），禁止每帧跑**。

## 第一批：修 3 个审计确认的 bug（P0 优先）

### 1.1（P0）啃一口穿过任何墙
- 现状：`doBite` 咬 1 子格后 `carved` 有记录 → `blocksPlayer()` 整格放行（玩家有子格碰撞兜底，**怪没有**）→ 木板墙 0.72s、石墙 1.2s 被穿，粘合墙一样。注释"铁=防御材料"不成立。
- 修法（择一，写清 rationale）：① `mobBlocked` 对 `carved` 且 `n < 500` 的格子仍视为实心（咬掉不足半=实心）；② 怪啃墙改为对同一格累计 ≥5 口（1.2s×硬度/3 每口）才放行。
- 粘合墙：允许被啃但 **2 倍咬数**（粘合=结构加固，不是免疫——免疫会让火失去存在意义）。
- 同步修正黄昏提示文案，别再说"没粘合的墙挡不住它们"这种与实际机制不符的话。

### 1.2（P1）火把=无敌结界
- 现状：怪在 0.85×4.5=3.8m 处掉头，攻击距离 1.25m——玩家站火把 2.6m 内**数学上永不被摸**，而 1 块煤=32 支=8 夜绝对安全。
- 修法（择一，写清 rationale）：① 恐惧 70% 生效，30% 概率"饿红眼"压住恐惧突进 2s（突进速度 ≤ 玩家快走 4.3，可甩开）；② 火把煤耗 ×3；③ 恐惧半径缩到 3m 且允许怪 1.35 倍速穿越恐惧圈 1s。
- 验收硬指标：**夜 5（≥5 只怪）站在火把边挂机 30s，被命中概率 > 0；火把旁 3m 内怪停留时间仍显著低于无火区**——火是显著安全区，不是无敌结界。

### 1.3（P1）怪击退被 AI 覆盖
- 现状：`tryAttack` 的 `best.vx += 4.5` 下一帧被 `m.vx = dirX*spd` 重写，击退只活 16ms。
- 修法：加 `m.kvx/m.kvz` 击退通道，每帧叠加进位移并 ×0.85 衰减；玩家的击退不动。

### 1.4（P2）两个小漏洞
- 怪啃墙的材料进玩家口袋（`doBite` 统计 frags）：给 `doBite` 加来源参数，怪啃的不入账。
- 夜怪不存档=刷新白嫖平安夜：至少在代码注释标为已知限制；顺手的话把 `mobs.length` 和位置存进存档（数值全 [PLACEHOLDER]）。

## 第二批：4 处注释与数学不符（只改注释或小数值，标注修正）

1. `atk 8=夜里挨三下会死` → 实际 100÷8=13 下。要么改注释，要么 atk 提到 12~15（写 rationale：单怪 100 血压多少秒、多怪围攻多久死）。
2. `铁大锤约 28` → 实际 24.7（3+2.588×7+32/9）。
3. 肌肉"±25%" → 公式 42/(10+m) 在 15kg 时 +68%。要么公式加钳制，要么注释改成实际区间。
4. "雪原夜 -18°C" → 温度是全局的，改成"雪天夜（任何群系）-18°C"。

## 第三批：补两个真实材料属性（每个数值带 rationale，未验证标 [PLACEHOLDER]）

### 3.1 可燃性 `burn`（0=不燃）
- BLOCKS 表加 `burn`：木板/木台阶/木梁 0.8、原木 0.6、树叶 1.0、火把营火自身不燃但为火源；石头/砂岩/铁/沙/雪/冰 0。
- 蔓延规则（挂 0.5s envTick）：燃源（火把 r=1.5m、营火 r=2m、燃烧中的块 r=1.2m）对相邻可燃块积累"受热点数"，点数超过该块 burn 的阈值 → 进入燃烧（燃烧 8~15s 后化为空气+烟粒子）。**燃烧块质量散失**——注释明写"燃烧=化学反应，质量守恒的唯一例外"，散失量 = 密度×90%。
- 联动：雨雪直接扑灭燃烧块（现有降雨系统）；玩家可挖掉燃烧块灭火（现有挖掘）。
- 验收：火把贴木板墙 30s 内引燃；石墙永不燃；雨天燃烧块自动熄；木质基地着火烧出洞后塌方系统正常接管。

### 3.2 导热性 `k`（保温系数，越小越保温）
- BLOCKS 表加 `k`：木板 0.2、原木 0.15、石料 0.35、砂岩 0.4、铁 0.9、雪 0.1（雪洞是真的能活命，冰 0.3）。
- 室内寒冷积累从固定 ×0.45 改为按屋顶材料的平均 k 加权：`shelterFactor = 0.15 + 0.85 × avgK`（全木板屋 ×0.32，全铁皮屋 ×0.92）——铁皮屋顶防怪但不保温，木板屋保温但脆。
- 验收：同尺寸屋顶，全木板屋寒冷积累比全铁矿石屋顶低 ≥50%；雪夜铁屋不生火会冻死，木屋能撑到天亮。

## 第四批：吸收 Minecraft 的成熟系统与设计实践（按性价比排序，4a 先做，4b 单独验证性能后再上）

Minecraft 十几年迭代已经替你踩过的坑，直接抄作业——但每一条都要写"借 MC 的什么、我们为什么取这个数"。

### 4a. 三个低成本高回报（直接做）

- **床——跳过夜晚**：木料 2 + 纤维材料 0.3 合成，放地上（noCollide）；夜晚对床按交互键，若 12m 内无夜行者 → 天色快进到黎明 + 冻结/饥饿照常结算；有怪 → 提示"有什么在附近游荡，睡不着"。**它制造的新决策：花一天 resources 换"永远不打夜战"——夜战派的战利品（肉）和睡觉派的安稳定价了这个博弈**。MC 原版规则：附近有怪不能睡，照抄。
- **村庄战利品箱**：每个村庄屋里工具箱旁生成一个补给箱（开一次消失）：火把 ×2 + 煤料 0.6kg + 苹果 ×1。给罗盘箭头一个目的地，给开局一个喘息。掉率固定不随机（确定性奖励，MC 战利品箱的随机在生存前期体验很差，我们不抄这部分）。
- **树苗——木材可再生**：挖树叶 10% 掉树苗（不可食用），对草地放置后 1~2 个游戏日长成小树。没有它，木材是消耗品，长期存档林子会被砍秃——MC 用随机刻（random tick）实现，我们复用现有 0.5s envTick 节拍低频抽签即可。

### 4b. 体素光照场（MC 最核心的技术资产，做完测帧率再决定去留）

- **借 MC 的什么**：15 级光照传播——天空光垂直下落 + 块光（火把 14 级）BFS 洪泛衰减，每格存 0~15 光照值。
- **拿来干什么**：①刷怪规则统一——只在光照 <6 的格子生成（替换现在"火把 6m 内不刷"的硬编码圈，火光形状从此是真实的光而不是圆形魔法结界）；②洞穴地下全黑（现在地下和地表一样亮，挖矿毫无恐惧感）；③光照值烘进区块网格顶点色（顶点色通道现成的）——洞穴变暗不花一个 draw call。
- **红线级约束**：必须**增量更新**（改一个方块只重算周围 15 格半径的 BFS 队列，MC 同款），禁止全图重算；数据是 Uint8Array(3.1M)（3MB，存档不用带它，重算即可）；帧预算超标 5% 就回退到现有光源池方案——**这条是唯一允许"做完再撤"的条目**。
- 验收：白天洞穴内光照=0（怪可刷）；火把插进洞穴，15 格内渐变照亮；挖开洞顶，天光斜射进来；帧率无回归。

### 明确不抄的（写下来防止它自由发挥）

红石电路（量级不匹配）、液体流动模拟（静态水够用）、村民交易（村庄还没有 NPC）、附魔（没有耐久系统之前无意义）、盔甲（等铁的用途先立住）。

## 红线（碰一条=返工）
1. 单文件、零新依赖；不拆文件、不引框架。
2. 不改核心数学（雕刻/质量守恒/塌方洪泛/攀爬绊倒）——可燃性和导热性都是在现有系统上**加系数**，不是重写。
3. 性能：draw calls < 120、三角面 < 80 万不得回退；蔓延/导热计算挂 0.5s 节拍；每帧热循环零对象分配。
4. 每个新数值带 rationale 注释，未验证标 [PLACEHOLDER · 假设+验证方法]；中文注释，风格延续。
5. 不删现有功能；`DEBUG.selfTest()` 的 16 项必须继续全过。

## 自测与交付
- `DEBUG.selfTest()` 扩充到 ≥28 项，新增：啃墙咬数门槛测试、火把非无敌测试（30s 内怪命中过玩家）、击退位移持续测试、可燃蔓延+雨灭测试、木板/铁皮屋顶保温差测试、床条件睡眠测试、树苗成长测试、（若做 4b）光照场增量更新正确性 + 刷怪光照判定测试、性能预算断言。
- 交付四样：①修复清单（每项：改动点/修法选择/rationale 3 行内）②新增数值表（数值/rationale/验证方法 三列，借 MC 的条目注明"借 MC：原值/我们的取值/为什么"）③selfTest 输出原文 ④已验证的实机操作步骤（给人工测试用，≤8 条）。
- 实施顺序：读审计报告 → diff 自查现状 → 第一批 bug → 批二注释 → 批三双属性 → 4a 三件套 → 跑全量自测 → 4b 光照场单独分支验证 → 汇报（含 4b 的帧率对比数据）。
